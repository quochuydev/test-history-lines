Fetch fresh crypto data, run analysis, then commit and push results.

Steps:
1. Run `yarn fetch` to download fresh cryptocurrency price data from Binance
2. Run `yarn dev` to analyze the data and find optimal trading opportunities
3. Stage all changes in `history/` and `results/` directories
4. Commit with message: "Update crypto data and analysis results"
5. Push to the current branch
